[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["src"]
omit = ["tests/*", "migrations/*"]
concurrency = ["greenlet", "thread"]

[tool.coverage.report]
show_missing = true

[tool.coverage.html]
directory = "htmlcov"
